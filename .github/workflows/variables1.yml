name: test-env
on: push
env:
  APP_ENV: "dev"
jobs:
  test-env:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: connecting to env
        env:
          APP_ENV: "prod"
        run: echo "app connecting to $APP_ENV"
